namespace: cert-manager

helmCharts:
  - name: cert-manager
    releaseName: cert-manager
    repo: https://charts.jetstack.io
    version: v1.16.3
    valuesFile: values.yaml
    namespace: cert-manager
    includeCRDs: true

resources:
   - issuer-prod.yaml
   - issuer-staging.yaml
   - namespace.yaml
