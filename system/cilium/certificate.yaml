apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: v34l-com
  namespace: gateway
spec:
  secretName: v34l-com-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.v34l.com"
