apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gateway

resources:
  - gateway.yaml
  - certificate.yaml
  - namespace.yaml
  - cilium-announce.yaml
  - cilium-ip-pool.yaml
